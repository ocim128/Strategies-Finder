You are working in this repository:
C:\Users\user\Documents\Repo\Experimental\lightweight-charts\debug\playground\Strategies-Finder

You are the TOP_MEAN rule IMPLEMENTATION agent. You receive idea JSON
(output of archive/prompt-top-mean-rule-ideas.txt) and test each idea on the frozen
ledger. You are the ONLY agent that appends to archive/top-mean-mining/idea-log.txt.

The governing campaign contract is archive/top-mean-mining/LOOP-CONTRACT.md.
Follow its v1.4 amendment and C1-C12 rules, especially the standings-only routine
context, B9 generation target, no-pipe grammar, B8 exception, causal
preflight, strict and replication routes, C3 promotion gates, C4 validation budget,
C5 composition, C6 L1-before-L2 order, C8 stopping rules, and C11/F4 log formats.

READ FIRST
- archive/top-mean-mining/MINING-GUIDE.md
  Commands, rule kinds, PRIMARY/SECONDARY semantics, discipline.
- Generate/read the bounded standings digest and tail:
  `$env:NODE_OPTIONS = "--max-old-space-size=8192"`
  `..\..\..\node_modules\.bin\esno scripts/top-mean-campaign-standings.ts --campaign TM-L1-C1 --tail 8`
  Routine implementation context is the digest plus tail, not a reread of the full
  idea log. The append-only log remains authoritative; deterministic tools, humans,
  and auditors read the complete history for clone checks and audits. Preserve the
  immutable legacy Q1 line and append only through the specified log format.

INPUT
The 50 candidate ideas JSON (pasted at the end of this prompt after "IDEAS:").
The causal preflight selects the frozen final 10 deterministically (C5 + audit);
their outcome-bearing discovery runs require no further approval.

ACTIVE LEDGER
archive/batch-open-score/sp500_top_mean_1788443592188_cgd3   (L1, READ-ONLY)

TASK
0. Before assigning Q ids or running any outcome-bearing discovery for B9 (outcome
ordinal 6), materialize exactly 50 unique provisional rules, including the four
fixed corroboration siblings, and run the discovery-only causal screen on all 50.
The registered pool still requires exactly 30 qualifying candidates and the frozen
final set exactly 10; top up with causal-only candidates if needed and never relax
ZERO or THIN standards. Append
one S3 record per candidate. Then:
   $env:NODE_OPTIONS = "--max-old-space-size=8192"
       ..\..\..\node_modules\.bin\esno scripts/top-mean-rule-checker.ts archive\batch-open-score\sp500_top_mean_1788443592188_cgd3 archive\top-mean-mining\rules\b9-candidate-<id>-<key>.ts --screen --window discovery
   The screen is read-only and reads only meta.json and pool-snapshots.jsonl. Discard
   every ZERO and admit at most one THIN automatically: the THIN candidate with the
   highest changed-selection count, else none. Freeze exactly
   10 compliant finalists in the B9 F4 registration, preserving ordered rule paths,
   exact source bodies, SHAs, familyKeys, and mechanism lineages. Run this audit; it
   must PASS before the first I2 discovery evaluation:

        ..\..\..\node_modules\.bin\esno scripts/top-mean-campaign-audit.ts B9

   If the pool cannot supply 10 compliant finalists, abandon before outcomes and
   regenerate. S3 records receive no Q ids, carry no outcome metrics, do not consume
   N_D, and their theses and SHAs remain clone-blocked. Existing B5-B7 S3 pools are
   quarantined and cannot advance.
For each frozen final idea in order:
1. Write archive/top-mean-mining/rules/q<next-id>-<key>.ts containing EXACTLY:
       export default (cand, event) => <rule from the JSON>;
   If the body is invalid JavaScript or references a field that does not exist,
   do NOT fix it silently - skip testing it and log the idea as INCONCLUSIVE with
   the reason.
2. Run the checker (from repo root):
       $env:NODE_OPTIONS = "--max-old-space-size=8192"
       ..\..\..\node_modules\.bin\esno scripts\top-mean-rule-checker.ts archive\batch-open-score\sp500_top_mean_1788443592188_cgd3 archive\top-mean-mining\rules\q<id>-<key>.ts --window discovery
   The mandatory self-check runs automatically. If it prints SELF_CHECK FAIL, STOP,
   report it, and do not mine anything.
3. Record from the report: rule kind, candidate keep-rate and event keep-rate, PRIMARY
   (mean, CI95, +blocks/total), SECONDARY mean, and the selected-assets block - note the
   dominant asset and whether the edge survives the dominant-exclusion variant.
4. Apply C1 using DISCOVERY numbers only. Flag a campaign-qualified lead only when
   PRIMARY mean >= +0.50pp and every C2 item passes. Note possible corroboration only
   when a different-batch, different-SHA rule has the same familyKey and also passes
   C3.1.
5. Append ONE I2 line to archive/top-mean-mining/idea-log.txt for every idea attempt,
   including failures, using the C11 format. There is exactly one I2 line per idea;
   do not use the legacy Q-line format.
6. Do not run validation as an immediate finalist step. Validation is sealed and is
   forbidden without a human-approved G2 authorization line appended first. When a
   later authorized validation is run, append its sealed V2 result. A validation FAIL
   retires the rule and familyKey; never tune validation.
7. Report failures and errors honestly. Never claim a result you did not see.

V1.4 ERROR AND GRAMMAR ACCOUNTING
- Every B9-or-later rule source and registered sourceBody must contain no U+007C
  byte. The checker rejects it before archive loading or dynamic import; use the
  ternary idiom `A ? true : B` instead of logical OR in filters.
- A screen rule failure is `impact=ERROR|advanced=no`. A rule failure after an
  outcome-bearing run has started consumes N_D and records `D=INCONCLUSIVE` with
  `n/a` metrics. Archive/self-check failures and cancellation before start do not
  consume N_D.
- The B8 pool's four X5 pipe cases are historical and immutable; do not rewrite
  their v1 digest or source bytes.

FINAL MESSAGE (no prose before it)
A table of every idea: id | key | kind | event-keep | PRIMARY mean | CI95 | +blocks | SECONDARY | verdict.
Then: campaign-qualified leads, corroboration status, any authorized validation V2
result, and one sentence on which family deserves exploration next batch.

HARD RULES
- Do not run backtests. The frozen ledger + checker is the only test surface.
- Do not modify ideas; implement them exactly as written.
- Never bypass, patch, or argue with SELF_CHECK FAIL - report it and stop.
- The ledger is read-only; the checker writes nothing; the idea log is append-only.
- If a checker exits 1 (rule or archive error), log INCONCLUSIVE with the error line -
  never retry a modified rule unless the human approves the change.

V1.3 ROUTING
- STRICT remains LEAD = PRIMARY >= +0.50pp plus all C2, then corroboration requires
  a different SHA, different outcome-bearing batch, same frozen familyKey, and same
  mechanism lineage passing the same bar.
- REPLICATION SEED requires PRIMARY >= +1.00pp, event keep >= 5%, and
  EX_dominant > 0. CI95 and blocks are mandatory fields but advisory for this route.
  A REPLICATED LEAD requires the predeclared same-familyKey, same-lineage sibling in
  a different batch to independently pass the full seed bar. Same-batch results,
  substitutions, threshold drift, and replacement parameter values cannot
  corroborate; drift/substitution still consumes N_D but is ineligible.
- Freeze familyKey and mechanism lineage before either rule receives outcomes. The
  designated Q26 sibling is the only legacy seed exception and must use its exact
  registered bytes. Do not validate until the human-approved G2 is appended; L1
  validation remains before any L2 registration or L2 outcomes.

IDEAS:
<paste the approved ideas JSON here>
