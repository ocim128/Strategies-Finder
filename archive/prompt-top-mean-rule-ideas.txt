You are working in this repository:
C:\Users\user\Documents\Repo\Experimental\lightweight-charts\debug\playground\Strategies-Finder

You are an expert quantitative researcher generating ASSET-SELECTION rule ideas for the
TOP_MEAN decision layer.

TASK
Generate exactly 50 NEW selector rule ideas as a causal-screening pool, not the
approved batch. Include any fixed corroboration siblings supplied below. A
downstream agent screens the pool and selects the final 10 satisfying C5 before
human approval.
You do not run the checker or modify any file. Idea generation only; the read-only
standings command below is the sole routine context lookup.

CONTEXT
The entry strategy and pair universe are frozen. On every decision event (2-3 per day,
4H S&P500 synthetic-pair surface, 136-asset catalog) the incumbent selector TOP_MEAN
picks the positive-score, longEligible asset with the highest score =
signedVotes / activePairCount and trades it vs USD for a fixed 24-bar horizon. Full
window: +5.40% mean per event vs +0.72% same-pool random control (delta +4.68%, n=938).
Research goal: keep everything frozen; mine rules for WHICH asset to pick or WHICH
candidates/events to allow, so the selection beats the incumbent on matched events
(PRIMARY).
Engine arms already implemented (anti-clone - never re-derive these): TOP_RAW (max
signedVotes, +4.86%), TOP_ADJUSTED (raw/sqrt(activePairs), +4.73%), TOP_MEAN
(incumbent, +4.68%), TOP_MEAN_RAW_UNIQUE_V1, TOP_MEAN_TREND (breadth>0.5 + ema200Above,
activePairs tie-break), REGIME_MEAN, MAX_ACTIVE (max activePairCount), MAX_SUBMITTED,
MAX_RETAINED, ACCELERATING, plus the FRESH/STALE/HHI/PAIRS conditional splits. The
current dominant selection asset is SNDK (most frequently picked by TOP_MEAN).

READ FIRST (mandatory)
- Generate/read the bounded digest and tail:
  `$env:NODE_OPTIONS = "--max-old-space-size=8192"`
  `..\..\..\node_modules\.bin\esno scripts/top-mean-campaign-standings.ts --campaign TM-L1-C1 --tail 8`
  Routine agents consume this deterministic `top_mean_standings.v1` output and
  do not reread the full idea log. The append-only log remains authoritative;
  deterministic tools, humans, and auditors read the complete history when
  performing clone checks or audits. Every thesis and familyKey already tried,
  with verdicts, is clone-blocked. NEVER propose a logged thesis, a rewording of
  one, the same feature with the same gate direction, or a combination of
  already-dead features. Also never propose anything equivalent to the incumbent
  (raw score ordering) or to a listed engine arm.
- archive/top-mean-mining/MINING-GUIDE.md
  Rule kinds, PRIMARY/SECONDARY semantics, discipline.

RULE KINDS (exactly one per idea)
- ranking: (cand, event) => number — the highest-value candidate wins the event.
- filter:  (cand, event) => boolean — true candidates remain; among them the frozen
  TOP_MEAN score decides. A filter can change the pick and can drop whole events
  (no true candidates = event not kept).

AVAILABLE FIELDS (causal, known at decision time; anything else THROWS)
cand:  asset, signedVotes, activePairCount, score (recomputed signedVotes/activePairCount),
       inPool, longEligible, shortEligible, ema200Above, breadth, regime, poolVersion
event: decisionTimeSec, breadth, regime, poolSize, dow (UTC, 1=Mon..5=Fri), hour (UTC)
NOTE: a rule sees ONE candidate at a time. Cross-candidate comparisons (gap to second
best, rank within event) are NOT expressible. Design accordingly.
FORBIDDEN: outcome/return data of any kind (sealed by the checker); alphabetical asset
rank carries no information. Asset-NAME conditioning is BANNED (contract v1.2): rules
must not reference cand.asset in any form — no exclusions, no per-asset weight
tables, no name-conditional gates. Asset identity has no causal mechanism; name-based
selection memorizes in-window winners (Q1 SNDK and Q2 APP both measured WORSE).

MEASURED SCALES ON L1 DISCOVERY (568 events, 35,557 candidates — calibrate BEFORE proposing)
score           p25 0.08 | p50 0.19 | p75 0.36 | p95 0.67 | p99 0.90 | max 1.0
signedVotes     p25 5 | p50 12 | p75 20 | p95 32 | p99 37 | max 40
activePairCount p25 55 | p50 60 | p75 64 | p95 71 | band 13..79, almost always 41+
breadth         p25 0.59 | p50 0.67 | p75 0.71 | p95 0.78 | max 0.84 (regime bullish when breadth > 0.5)
poolSize is constant 136. Events/day: 2-3 (4H session bars). dow values 1..5 only.
A threshold matching ~0% or ~100% of candidates tests nothing. Filter event keep-rates
below 5% are fragile (few events = inconclusive).

MEASURED AMPLITUDE LAWS (v1.4)
Ranking perturbations of roughly +/-10% are presumed THIN unless the flip witness
targets an exact score tie (15.14% of events) or a sub-0.025 score margin (21.13%).
Prefer coverage boundaries in the 41-55 winner frontier, strong two-sided scales,
discontinuities, or joint gates. Event-only terms, shrinkage variants,
vote-monotone equivalents, identity rules, and threshold variants of logged theses
are rejected. The pipe character U+007C is forbidden anywhere in a rule; use a
ternary idiom such as `A ? true : B`. Every witness must use feasible integer values
and show a computed reversal.

TOP-OF-BOOK CALIBRATION (use this for selector design instead of inferring from all
candidates)
winner activePairCount p0 16 | p25 41 | p50 44 | p75 45 | p95 48 | max 55
runner-up activePairCount p0 38 | p25 42 | p50 46 | p75 49 | p95 53 | max 59
top-1 score p25 0.7447 | p50 0.8222 | p75 0.9024
top-2 score p25 0.60 | p50 0.7333 | p75 0.8537
exact ties 86/568 (15.14%); margin <=0.01 112/568 (19.72%); <=0.025 120/568 (21.13%)
max-signedVotes vs TOP_MEAN differ 20/568 (3.52%)
batch-1 causal changes: Q3 0, Q4 0, Q6 0, Q7 1, Q9 0, Q11 11 of 568
Candidate keep-rate is not selection impact.

BOUNDED SCORE-REWEIGHTING (mechanism-distinct only)
Bounded score leverage and discount forms are allowed only when their mechanism
lineage is declared before screening and is distinct from every logged thesis. They
are ranking rules: candidate-specific multipliers and functions of the causal fields
are allowed and DO break the invariance law. Open-ended reweighting and threshold
sweeps are not an invitation to generate variants.
- Denominator cap: cand.signedVotes / Math.min(cand.activePairCount, CAP) — stop
  counting coverage beyond a cap; useful CAP values sit near the winner frontier
  (41..60), not the candidate band (55..79).
- Bayesian shrinkage: (cand.signedVotes + K) / (cand.activePairCount + M) — blend the
  raw ratio toward a prior support level instead of trusting small denominators.
- Conditioned exponents/weights: bounded coverage- or trend-scaled score terms.
Asset-NAME leverage (per-asset weight tables) is RETIRED (contract v1.3): it is the
identity family in continuous form and memorizes in-window winners (~136 free
parameters). Broad `coverage:coverage` exploration is also RETIRED; the sole
exception is the designated Q26 lineage pilot below. Weighting must be driven by
causal features (support, trend, regime), never by asset identity.
Every reweighting idea still needs a numeric flip witness naming which frontier
events it flips (low-coverage winner vs high-coverage runner-up, e.g. 14 votes/16
pairs losing to 27 votes/51 pairs).

HARD DESIGN RULES FOR THIS CAMPAIGN
1. CAUSAL ONLY. Only the fields listed. The checker enforces this by throwing.
2. ANTI-CLONE. The idea log is the law. Generic single-feature gates with an obvious
   direction are expected to land at noise level; prefer INTERACTIONS of two independent
   information sources (coverage x trend, breadth x score-shape, calendar x regime) and
   nonlinear score transforms (saturation, floor effects) over obvious gates.
3. C5 BATCH COMPOSITION. Every batch has exactly 10 ideas, at least 6 distinct
   familyKey values, at most 2 ideas per familyKey, and NO identity/asset-list rule
   or broad `coverage:coverage` familyKey (retired v1.3),
   and at most 1 combination. An EDGE cannot spawn a same-batch variant.
4. C7 COMBINATIONS. A combination has exactly two parents, both pass the campaign
   discovery floor and concentration checklist, the parents have different familyKeys
   and distinct causal sources, neither parent is a combination, and the fresh idea
   records both parents. Combination ideas require human approval at campaign review.
5. NON-TRIVIALITY. A ranking rule must be a genuinely different ordering than
   score / signedVotes / activePairCount / raw-sqrt(activePairs) (else it clones an
   engine arm). A filter must state its expected event keep-rate; ~100% keep is only
   acceptable for a targeted exclusion thesis.
6. Each idea is ONE one-line rule body (the wrapper is added at implementation):
   <rule over cand and event>
7. Generate 50 candidates per batch, including fixed corroboration siblings, then
   screen all 50 causally before freezing the registered 30 and final 10.

SELECTION-INVARIANCE LAW
A ranking changes an event only if another candidate beats the frozen winner under
(rule value, FNV digest). Any rule of the form g_event(cand.score) with g_event
strictly increasing is banned because it preserves ordering and ties. Event-only
terms, positive event-only multipliers, and event-only offsets cannot change
within-event ordering. A filter changes the pick only if it rejects the winner and
retains an alternative. Pure event filters have PRIMARY exactly zero on retained
events and are banned. cand.longEligible is always true and cand.score is always
positive in the base pool. Monotone-in-signedVotes is not banned by itself (there is
no quota).

MANDATORY FLIP WITNESS
Every idea must provide arithmetic in flipArgument. For a ranking, give two feasible
candidate tuples A/B with score(A) > score(B) but rule(A) < rule(B), with values
computed, or give an exact tie that the rule resolves differently. For a filter,
give an incumbent-like tuple rejected and an alternative-like tuple retained. “May
reorder” is not a witness.

BATCH MECHANISM MIX (applies to the screened final 10)
- At least 6 rankings and at least 2 candidate filters.
- At least 6 distinct familyKeys.
- At least 2 rankings use a bounded candidate-specific secondary signal rather than
  another denominator curve.
- No identity rule, no broad `coverage:coverage` familyKey, and no combination.
- A causal ZERO rule is rejected.
- At most one THIN rule, with explicit human approval.

V1.3 ROUTING AND B8 BATCH LOCK
- Identity and broad `coverage:coverage` exploration are RETIRED. Do not generate
  open-ended score reweighting or threshold-sweep variants. Any reweighting must be
  bounded, mechanism-distinct, and predeclared.
- The fixed designated candidate counts toward the 30-candidate B8 pool and must
  appear verbatim:
  key=`q26_sibling_low_breadth_coverage_floor_55`, kind `filter`,
  family=`interaction:interaction`, mechanism=`candidate-filter`, rule:
  `event.breadth < 0.62 ? cand.activePairCount >= 55 : true`
- The other 29 candidates must not clone any thesis in `idea-log.txt`, including
  quarantined B5-B7 S3 records. No second Q26 threshold or functional variant is
  allowed: do not sweep 54/56/58/60 or any equivalent threshold. No identity and no
  broad `coverage:coverage` familyKey.
- A replication seed requires PRIMARY >= +1.00pp, event keep >= 5%, and
  EX_dominant > 0. CI95 and block counts remain mandatory report fields but are
  advisory for routing. A replicated lead requires a different-SHA rule in a
  different outcome-bearing batch, the same frozen familyKey and the same
  predeclared mechanism lineage, independently passing the full seed bar. Declare
  and freeze familyKey and mechanism lineage before either rule receives outcomes.
  Same-batch results cannot corroborate; a failed sibling cannot be replaced by a
  different parameter value. C2 remains mandatory for STRICT and every fresh
  confirmation; routing gates are not promotion evidence.
- The v1.3 semantics apply before B8: outcome-batch ordinal 5 is B8, while existing
  B5-B7 S3 pools are immutable, clone-blocked, and quarantined from advancement.

V1.4 GENERATION AND CONTEXT LOCK
- Before B9, use the standings digest and tail for routine context. Do not reread
  the full log during ordinary idea generation.
- Target 50 total causal-screen candidates per batch, including the four fixed B9
  corroboration siblings below. The registered pool remains exactly 30 qualifying
  candidates and the frozen finalist set exactly 10; top up with causal-only ideas
  if fewer than 30 qualify, without relaxing ZERO or THIN standards.
- Rules and registered source bodies may not contain U+007C. Use `A ? true : B`
  instead of logical OR in filters. The full-history checker rejects duplicate
  keys, exact/canonicalized bodies, SHA collisions, known theses, family-cap reuse,
  identity references, and undeclared lineage.

FIXED B9 CORROBORATION SIBLINGS (verbatim)
Freeze exactly these four candidates before outcomes, one for each current strict
lead. They are fixed candidates, not threshold variants; do not alter their keys,
familyKeys, lineages, or rule bodies.

Q29 sibling (regime-breadth:bear_coverage_floor):
event.regime === "bearish" ? cand.score * (cand.activePairCount >= 48 ? 1.15 : 0.85) : cand.score

Q31 sibling (regime-breadth:bear_dual_support_floor):
event.regime === "bearish" ? cand.score * (cand.activePairCount >= 48 && cand.signedVotes >= 15 ? 1.15 : 0.85) : cand.score

Q32 sibling (interaction:support_deficit_votes):
cand.signedVotes - Math.pow(Math.max(0, 55 - cand.activePairCount), 2) / 22

Q35 sibling (interaction:quality_confirmation_premium):
cand.score + (cand.ema200Above ? (cand.activePairCount >= 50 ? 0.20 : 0) : 0)

NON-NEGOTIABLE OUTPUT RULES
- The ideas array must contain exactly 50 candidate ideas.
- Return valid JSON only.
- Do not use markdown fences.
- Do not write prose outside the JSON.
- The top-level object must be:
{
  "ideas": [
    {
      "key": "snake_case_key",
      "rule": "rule body only, e.g. cand.signedVotes >= 20 ? true : cand.ema200Above",
      "kind": "ranking|filter",
      "familyKey": "family:specific_family_key",
      "mechanism": "ranking-reorder|candidate-filter:lineage",
      "flipArgument": "arithmetic witness that the rule can change selection",
      "thesis": "question form, one sentence"
    }
  ]
}
