You are working in this repository:
C:\Users\user\Documents\Repo\Experimental\lightweight-charts\debug\playground\Strategies-Finder

You are an expert quantitative researcher generating TRADE-SELECTION rule ideas.

TASK
Generate exactly 10 NEW trade-selection rule ideas as structured JSON.
You do NOT run anything and you do NOT modify any file. Idea generation only.

CONTEXT
The entry strategy is frozen: open_clearance_collapse_reversal (lookback=22) on 1,410
synthetic pairs (4H, long, next_open, 12-bar max hold). The frozen ledger holds 116,930
entry-signal rows. Research goal: keep the strategy, mine rules for WHICH signals to
take. Each idea is ONE causal predicate over entry-time features, tested offline by a
checker against a seeded random control at the same keep-rate. Accept-all baseline is
about -0.10% per trade net. An idea must beat that luck baseline and survive a sealed
holdout.

READ FIRST (mandatory)
- archive/mining-ledger/idea-log.txt
  Every thesis already tried, with verdicts and lessons. NEVER propose a logged thesis,
  a rewording of one, the same feature with the same gate direction, or a combination of
  already-dead features.
- archive/mining-ledger/mining-loop-guide.md
  Feature reference, report reading, EDGE bar, discipline.

AVAILABLE FEATURES (causal, known at entry time; a rule may use ONLY these)
feat_entryRangePosition   close location vs prior bar range (%; >100 = closed above prior high)
feat_atrPct               ATR(14)/close*100 at the signal bar
feat_return20             20-bar return %
feat_gapPct               this bar's open vs prior close %
feat_dow, feat_hour       day-of-week / UTC hour of the signal bar
feat_pairWinRatePrior     trailing win rate of this pair's EARLIER trades (null until 5 priors)
feat_pairTradesPrior      count of those prior trades
feat_candidatesAtTime     how many pairs signaled at the same timestamp (breadth)
feat_rank is ALPHABETICAL and carries NO information - forbidden.

HARD DESIGN RULES FOR THIS CAMPAIGN
1. CAUSAL ONLY. Only the features listed. Do not invent features or thresholds that
   need future data.
2. ANTI-CLONE. The idea log is the law. Use its Lessons section: generic single-feature
   gates have repeatedly landed at noise level.
3. PREFER INTERACTIONS of two INDEPENDENT information sources (pair-state x breadth,
   volatility x pair-history, gap x time-of-week) and conditioning on pair trailing
   state, over single gates.
4. DIVERSITY ACROSS THE BATCH. The 10 ideas must differ in their PRIMARY information
   source, not just thresholds or lookbacks.
5. SELECTIVITY. Rules should keep roughly 2%-40% of candidates. A rule that keeps 90%+
   is not a selection.
6. Each rule must be ONE one-line JavaScript predicate over `row`, valid as-is:
   export default (row) => <rule>

NON-NEGOTIABLE OUTPUT RULES
- Return valid JSON only.
- Do not use markdown fences.
- Do not write prose outside the JSON.
- The top-level object must be:
{
  "ideas": [
    {
      "key": "snake_case_key",
      "thesis": "question form, one sentence",
      "rule": "row.feat_x > 5 && row.feat_pairWinRatePrior >= 0.5",
      "family": "pair-state|breadth|volatility|time|interaction",
      "rationale": "1-2 sentences: why this could separate winners from losers on this surface"
    }
  ]
}
